/**
 * Audience boundary for an Activity record.
 */
export enum ActivityVisibility {
  Private = "private",
  Internal = "internal",
  Workspace = "workspace",
  Company = "company",
  Public = "public",
}
