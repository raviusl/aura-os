/**
 * Database types package entry (Sprint 002 foundation).
 *
 * Canonical schema types live in `src/types/database.ts`.
 * This folder provides the `types/database/` package layout.
 */
export type * from "../database";
