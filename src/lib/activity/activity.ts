/**
 * Activity foundation surface (Project 017).
 * Definitions only — no timeline rendering or activity generation.
 */
export type {
  Activity,
  ActivityId,
  ActivityMetadata,
} from "@/types/activity";
