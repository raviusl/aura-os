/**
 * Audit foundation surface (Project 017).
 * Definitions only — no persistence or security policy.
 */
export {
  AUDIT_ACTION_CODES,
  AUDIT_RESULT_CODES,
  type AuditAction,
  type AuditActionCode,
  type AuditActionId,
  type AuditLog,
  type AuditLogId,
  type AuditResult,
  type AuditResultCode,
  type AuditResultId,
  type AuditValue,
} from "@/types/activity";
