/**
 * Permanent Activity domain surface (Project 018).
 * Models only — no generation, persistence, or authorization behavior.
 */
export type {
  Activity,
  ActivityActor,
  ActivityActorId,
  ActivityCategory,
  ActivityCategoryId,
  ActivityId,
  ActivityMetadata,
  ActivityTarget,
} from "@/types/activity";

export {
  ActivityAction,
  ActivityActorType,
  ActivityResult,
  ActivityTargetType,
  ActivityVisibility,
} from "@/types/activity";
