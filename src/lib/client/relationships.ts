/**
 * Client relationship foundation surface.
 * References only — no relationship loading or mutation.
 */
export type {
  ClientContact,
  ClientProject,
  ClientProjectRelationship,
  ClientReference,
} from "@/types/client";
