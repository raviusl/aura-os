/**
 * People identity foundation surface (Project 007).
 * Definitions only — runtime operations remain in core/people.
 */
export {
  PERSON_STATUSES,
  type Person,
  type PersonId,
  type PersonStatus,
} from "@/types/people";
