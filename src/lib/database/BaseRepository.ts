import "server-only";

/**
 * Abstract data-access root (Sprint 002 foundation).
 * Domain repositories continue to live under src/core/{domain}/repository.ts.
 */
export abstract class BaseRepository {
  protected constructor() {}
}
