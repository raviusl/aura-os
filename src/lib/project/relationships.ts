/**
 * Project relationship foundation surface.
 * References only — no loading, mutation, or workflow behavior.
 */
export type {
  ProjectMember,
  ProjectReference,
  ProjectTimeline,
} from "@/types/project";
