/**
 * Workspace member foundation surface (Sprint 004).
 * Definitions only — membership persistence remains in core/membership.
 */
export {
  WORKSPACE_INVITATION_STATUSES,
  type WorkspaceInvitationStatus,
  type WorkspaceMember,
} from "@/types/workspace";
