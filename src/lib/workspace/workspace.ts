/**
 * Workspace foundation surface (Sprint 004).
 * Definitions only — CRUD lives in existing core/workspace modules.
 */
export {
  WORKSPACE_STATUSES,
  type Workspace,
  type WorkspaceId,
  type WorkspaceStatus,
} from "@/types/workspace";
