/**
 * Workspace settings foundation surface (Sprint 004).
 * Definitions only — no settings persistence or UI.
 */
export type {
  WorkspaceBrandingSettings,
  WorkspaceLocaleSettings,
  WorkspacePreferences,
  WorkspaceSettings,
  WorkspaceTimezoneSettings,
} from "@/types/workspace";
