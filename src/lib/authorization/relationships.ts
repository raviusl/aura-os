/**
 * Authorization relationship foundation surface (Project 008).
 * References only — no assignment or permission checking.
 */
export type {
  CompanyRole,
  PermissionReference,
  ProjectId,
  ProjectRole,
  RoleAssignment,
  WorkspaceRole,
} from "@/types/authorization";
