/**
 * Timeline foundation surface (Project 011).
 * Definitions only — no scheduling or calendar behavior.
 */
export {
  type Milestone,
  type MilestoneId,
  type Timeline,
  type TimelineId,
} from "@/types/timeline";
