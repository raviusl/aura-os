/**
 * Timeline Reminder foundation surface.
 * Definitions only — no reminder or notification engine.
 */
export type {
  Reminder,
  ReminderId,
  ReminderType,
  ReminderTypeId,
} from "@/types/timeline";
